services:
  - type: web
    name: nephrolog-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn wfp_fastapi_bot:app --host 0.0.0.0 --port 10000
